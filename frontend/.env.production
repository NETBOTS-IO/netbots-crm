# Set this to your production backend API URL.
# If your frontend is served from the same server/domain as your backend, you can leave it empty to use relative paths (/api)
VITE_API_BASE_URL=https://api.netbots.io/api
